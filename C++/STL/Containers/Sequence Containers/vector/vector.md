```cpp
#include <vector> 
using namespace std; 

vector<char> vowels; 
vector<char> vowels(5); 
vector<char> vowels {'a', 'e', 'i', 'o', 'u'};

vector<int> rating(5);
rating[0] = 9;

vector<Shape*> shapes;
```